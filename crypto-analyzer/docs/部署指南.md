# Crypto Analyzer Linux 部署指南

## 概述

本指南详细说明如何使用 `deploy.sh` 脚本在全新的 Linux 服务器上部署 Crypto Analyzer 系统。该脚本会自动安装所有依赖并完成系统配置。

## 支持的操作系统

- Ubuntu 20.04 / 22.04
- Debian 11 / 12
- CentOS 7 / 8
- RHEL 7 / 8

## 前置条件检查

### 1. 系统要求

- **最低配置**：
  - CPU: 2核
  - 内存: 4GB RAM
  - 磁盘: 20GB 可用空间
  - 网络: 稳定的互联网连接

- **推荐配置**：
  - CPU: 4核或更多
  - 内存: 8GB RAM 或更多
  - 磁盘: 50GB SSD
  - 网络: 10Mbps+ 带宽

### 2. 权限要求

- 需要 **root 权限**或具有 sudo 权限的用户
- 确保可以执行系统软件包安装

### 3. 网络要求

- 服务器需要访问以下资源：
  - Python 官方源（下载 Python 3.11）
  - 操作系统软件包仓库
  - PyPI（Python 包索引）
  - 加密货币交易所 API（Binance、Gate.io 等）

## 部署步骤

### 步骤 1: 准备代码

**方式一：从 Git 克隆（推荐）**

```bash
# 安装 git（如果尚未安装）
sudo apt-get install git  # Ubuntu/Debian
# 或
sudo yum install git       # CentOS/RHEL

# 克隆项目
git clone <your-repository-url> /opt/crypto-analyzer
cd /opt/crypto-analyzer
```

**方式二：上传代码包**

```bash
# 使用 scp 上传
scp -r crypto-analyzer/ user@server:/opt/

# 或使用 FTP/SFTP 工具上传
# 上传后解压到目标目录
```

### 步骤 2: 配置部署参数（可选）

在运行部署脚本前，您可以修改 `deploy.sh` 中的配置变量：

```bash
vim deploy.sh
```

主要配置项：

```bash
PYTHON_VERSION="3.11"                          # Python版本
MYSQL_ROOT_PASSWORD="CryptoAnalyzer@2024"      # MySQL root密码
MYSQL_DATABASE="crypto_analyzer"               # 数据库名
MYSQL_USER="crypto_user"                       # 数据库用户
MYSQL_PASSWORD="crypto_pass_2024"              # 数据库密码
APP_PORT="9020"                                # 应用端口
NGINX_PORT="80"                                # Nginx端口
```

**安全建议**：强烈推荐修改默认密码！

### 步骤 3: 运行部署脚本

```bash
# 添加执行权限
chmod +x deploy.sh

# 使用 root 权限运行
sudo bash deploy.sh
```

### 步骤 4: 确认部署配置

脚本会显示部署配置并要求确认：

```
===== 部署配置确认 =====
项目目录: /opt/crypto-analyzer
MySQL数据库: crypto_analyzer
MySQL用户: crypto_user
应用端口: 9020
Nginx端口: 80
==========================

确认开始部署? (y/n):
```

输入 `y` 确认开始部署。

### 步骤 5: 等待部署完成

脚本会自动执行以下步骤（大约需要 10-30 分钟）：

1. ✅ 更新系统软件包
2. ✅ 安装 Python 3.11
3. ✅ 安装 MySQL 8.0
4. ✅ 安装 Nginx
5. ✅ 配置 Python 虚拟环境
6. ✅ 配置数据库连接
7. ✅ 初始化数据库表结构
8. ✅ 配置 Systemd 服务
9. ✅ 配置 Nginx 反向代理
10. ✅ 配置防火墙
11. ✅ 创建管理脚本
12. ✅ 启动服务

## 部署后验证

### 1. 检查服务状态

```bash
# 运行状态检查脚本
./status.sh

# 或使用 systemctl
sudo systemctl status crypto-analyzer
```

### 2. 检查端口监听

```bash
# 检查应用端口（9020）
sudo netstat -tlnp | grep 9020

# 检查 Nginx 端口（80）
sudo netstat -tlnp | grep 80
```

### 3. 测试 Web 访问

```bash
# 本机测试
curl http://localhost

# 远程测试（替换为实际 IP）
curl http://your-server-ip
```

### 4. 查看日志

```bash
# 应用日志
tail -f /var/log/crypto-analyzer.log

# 或使用管理脚本
./logs.sh

# 系统日志
journalctl -u crypto-analyzer -f
```

## 系统管理

部署完成后，可使用以下管理脚本：

### 启动服务

```bash
./start.sh
# 或
sudo systemctl start crypto-analyzer
```

### 停止服务

```bash
./stop.sh
# 或
sudo systemctl stop crypto-analyzer
```

### 重启服务

```bash
./restart.sh
# 或
sudo systemctl restart crypto-analyzer
```

### 查看状态

```bash
./status.sh
```

### 查看日志

```bash
./logs.sh
```

## 配置文件说明

### 1. 应用配置文件

**位置**：`/opt/crypto-analyzer/config.yaml`

主要配置项：

```yaml
# 交易对配置
trading_pairs:
  - BTC
  - ETH
  - SOL

# 数据库配置（已由部署脚本自动配置）
database:
  host: localhost
  port: 3306
  user: crypto_user
  password: crypto_pass_2024
  database: crypto_analyzer

# 交易所 API 配置（需要手动填写）
exchanges:
  binance:
    api_key: "YOUR_BINANCE_API_KEY"
    api_secret: "YOUR_BINANCE_API_SECRET"

  gate:
    api_key: "YOUR_GATE_API_KEY"
    api_secret: "YOUR_GATE_API_SECRET"
```

### 2. Nginx 配置

**位置**：`/etc/nginx/sites-available/crypto-analyzer`

如需自定义 Nginx 配置（如 SSL、域名等），编辑此文件后重载 Nginx：

```bash
sudo nginx -t           # 测试配置
sudo systemctl reload nginx  # 重载配置
```

### 3. Systemd 服务配置

**位置**：`/etc/systemd/system/crypto-analyzer.service`

修改后需重载 systemd：

```bash
sudo systemctl daemon-reload
sudo systemctl restart crypto-analyzer
```

## 常见问题排查

### 问题 1: 服务无法启动

**症状**：`systemctl status crypto-analyzer` 显示 `failed`

**排查步骤**：

```bash
# 查看详细错误日志
journalctl -u crypto-analyzer -n 100 --no-pager

# 检查端口占用
sudo netstat -tlnp | grep 9020

# 检查数据库连接
mysql -u crypto_user -p -h localhost crypto_analyzer
```

**常见原因**：
- 端口被占用
- 数据库连接失败
- Python 依赖缺失
- 配置文件错误

### 问题 2: 无法访问 Web 界面

**症状**：浏览器无法打开 `http://server-ip`

**排查步骤**：

```bash
# 检查 Nginx 状态
sudo systemctl status nginx

# 检查防火墙
sudo ufw status              # Ubuntu
sudo firewall-cmd --list-all # CentOS

# 检查 Nginx 日志
tail -f /var/log/nginx/crypto-analyzer-error.log
```

**解决方案**：
- 确保防火墙开放 80 端口
- 检查 Nginx 配置是否正确
- 确认应用服务正在运行

### 问题 3: 数据库连接失败

**症状**：日志显示 "Can't connect to MySQL server"

**排查步骤**：

```bash
# 检查 MySQL 服务
sudo systemctl status mysql

# 测试数据库连接
mysql -u crypto_user -p crypto_analyzer

# 检查数据库用户权限
mysql -u root -p
mysql> SELECT user, host FROM mysql.user;
mysql> SHOW GRANTS FOR 'crypto_user'@'localhost';
```

**解决方案**：
- 确保 MySQL 服务运行正常
- 验证用户名和密码
- 检查用户权限

### 问题 4: Python 依赖安装失败

**症状**：pip install 过程报错

**排查步骤**：

```bash
# 激活虚拟环境
source venv/bin/activate

# 手动安装依赖
pip install -r requirements.txt -v

# 检查 pip 源
pip config list
```

**解决方案**：
- 使用国内 pip 镜像：
  ```bash
  pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
  ```
- 单独安装失败的包
- 检查系统依赖（gcc, python-dev 等）

### 问题 5: 内存不足

**症状**：系统卡顿或服务被 OOM Killer 终止

**排查步骤**：

```bash
# 检查内存使用
free -h
htop

# 检查系统日志
dmesg | grep -i "out of memory"
```

**解决方案**：
- 增加 swap 空间
- 升级服务器配置
- 优化应用配置（减少并发数、数据收集频率等）

## 性能优化建议

### 1. 数据库优化

```bash
# 编辑 MySQL 配置
sudo vim /etc/mysql/mysql.conf.d/mysqld.cnf

# 添加优化参数
[mysqld]
innodb_buffer_pool_size = 2G        # 设置为物理内存的 50-70%
innodb_log_file_size = 256M
max_connections = 200
query_cache_size = 64M
```

重启 MySQL：
```bash
sudo systemctl restart mysql
```

### 2. Nginx 优化

```bash
# 编辑 Nginx 配置
sudo vim /etc/nginx/nginx.conf

# 优化参数
worker_processes auto;
worker_connections 2048;
keepalive_timeout 65;
gzip on;
gzip_comp_level 5;
```

### 3. 应用优化

编辑 `config.yaml`：

```yaml
# 调整数据收集频率
collectors:
  interval: 300  # 秒，根据需求调整

# 调整并发数
async:
  max_workers: 10  # 根据 CPU 核心数调整
```

## 安全加固

### 1. 修改默认密码

```bash
# 修改 MySQL root 密码
mysql -u root -p
mysql> ALTER USER 'root'@'localhost' IDENTIFIED BY 'new_strong_password';

# 修改应用数据库密码
mysql> ALTER USER 'crypto_user'@'localhost' IDENTIFIED BY 'new_strong_password';

# 更新 config.yaml 中的密码
vim config.yaml
```

### 2. 配置 HTTPS（推荐）

使用 Let's Encrypt 免费 SSL 证书：

```bash
# 安装 certbot
sudo apt-get install certbot python3-certbot-nginx  # Ubuntu
sudo yum install certbot python3-certbot-nginx      # CentOS

# 获取证书（替换为实际域名）
sudo certbot --nginx -d your-domain.com

# 自动续期
sudo certbot renew --dry-run
```

### 3. 配置防火墙规则

```bash
# UFW (Ubuntu)
sudo ufw default deny incoming
sudo ufw default allow outgoing
sudo ufw allow ssh
sudo ufw allow 80/tcp
sudo ufw allow 443/tcp
sudo ufw enable

# Firewalld (CentOS)
sudo firewall-cmd --permanent --remove-service=dhcpv6-client
sudo firewall-cmd --permanent --add-service=http
sudo firewall-cmd --permanent --add-service=https
sudo firewall-cmd --reload
```

### 4. 设置日志轮转

```bash
# 创建日志轮转配置
sudo vim /etc/logrotate.d/crypto-analyzer

# 添加配置
/var/log/crypto-analyzer*.log {
    daily
    rotate 30
    compress
    delaycompress
    notifempty
    create 0640 www-data www-data
    sharedscripts
    postrotate
        systemctl reload crypto-analyzer > /dev/null
    endscript
}
```

## 数据备份

### 1. 数据库备份脚本

创建备份脚本：

```bash
vim ~/backup_db.sh
```

内容：

```bash
#!/bin/bash
BACKUP_DIR="/var/backups/crypto-analyzer"
DATE=$(date +%Y%m%d_%H%M%S)
MYSQL_USER="crypto_user"
MYSQL_PASSWORD="crypto_pass_2024"
MYSQL_DATABASE="crypto_analyzer"

mkdir -p $BACKUP_DIR

# 备份数据库
mysqldump -u $MYSQL_USER -p$MYSQL_PASSWORD $MYSQL_DATABASE \
    | gzip > $BACKUP_DIR/crypto_analyzer_$DATE.sql.gz

# 保留最近 30 天的备份
find $BACKUP_DIR -name "*.sql.gz" -mtime +30 -delete

echo "Backup completed: crypto_analyzer_$DATE.sql.gz"
```

设置定时备份：

```bash
chmod +x ~/backup_db.sh

# 添加到 crontab（每天凌晨 2 点备份）
crontab -e
# 添加：
0 2 * * * /root/backup_db.sh >> /var/log/db_backup.log 2>&1
```

### 2. 配置文件备份

```bash
# 备份配置
cp config.yaml config.yaml.backup
cp -r config/ config_backup/

# 定期备份到远程服务器
rsync -avz /opt/crypto-analyzer/config/ backup-server:/backups/crypto-analyzer/
```

## 监控建议

### 1. 系统监控

安装监控工具：

```bash
# 安装 htop
sudo apt-get install htop  # Ubuntu
sudo yum install htop      # CentOS

# 安装 Netdata（可选）
bash <(curl -Ss https://my-netdata.io/kickstart.sh)
```

### 2. 日志监控

```bash
# 实时监控错误日志
tail -f /var/log/crypto-analyzer-error.log | grep -i error

# 统计错误数量
grep -i error /var/log/crypto-analyzer.log | wc -l
```

### 3. 性能监控

```bash
# CPU 和内存使用
top -b -n 1 | head -20

# 磁盘使用
df -h

# 网络连接
netstat -an | grep ESTABLISHED | wc -l
```

## 升级和更新

### 1. 应用更新

```bash
# 停止服务
./stop.sh

# 拉取最新代码
git pull origin master

# 激活虚拟环境
source venv/bin/activate

# 更新依赖
pip install -r requirements.txt --upgrade

# 重启服务
./restart.sh
```

### 2. 系统更新

```bash
# Ubuntu/Debian
sudo apt-get update
sudo apt-get upgrade

# CentOS/RHEL
sudo yum update
```

## 卸载

如需完全卸载系统：

```bash
# 停止并删除服务
sudo systemctl stop crypto-analyzer
sudo systemctl disable crypto-analyzer
sudo rm /etc/systemd/system/crypto-analyzer.service

# 删除 Nginx 配置
sudo rm /etc/nginx/sites-enabled/crypto-analyzer
sudo rm /etc/nginx/sites-available/crypto-analyzer
sudo systemctl reload nginx

# 删除数据库
mysql -u root -p
mysql> DROP DATABASE crypto_analyzer;
mysql> DROP USER 'crypto_user'@'localhost';
mysql> DROP USER 'crypto_user'@'%';

# 删除项目文件
sudo rm -rf /opt/crypto-analyzer

# 删除日志
sudo rm -f /var/log/crypto-analyzer*.log
```

## 技术支持

如遇到问题，请：

1. 查看本文档的"常见问题排查"部分
2. 检查日志文件获取详细错误信息
3. 查阅项目文档和 README
4. 提交 Issue 到项目仓库

## 附录：快速命令参考

```bash
# 服务管理
sudo systemctl start crypto-analyzer     # 启动
sudo systemctl stop crypto-analyzer      # 停止
sudo systemctl restart crypto-analyzer   # 重启
sudo systemctl status crypto-analyzer    # 状态

# 日志查看
tail -f /var/log/crypto-analyzer.log            # 应用日志
tail -f /var/log/nginx/crypto-analyzer-error.log # Nginx错误日志
journalctl -u crypto-analyzer -f                 # Systemd日志

# 数据库管理
mysql -u crypto_user -p crypto_analyzer          # 连接数据库
mysqldump -u crypto_user -p crypto_analyzer > backup.sql  # 备份

# 配置文件
vim /opt/crypto-analyzer/config.yaml             # 应用配置
vim /etc/nginx/sites-available/crypto-analyzer   # Nginx配置
vim /etc/systemd/system/crypto-analyzer.service  # 服务配置
```

---

**祝您部署顺利！** 🚀
