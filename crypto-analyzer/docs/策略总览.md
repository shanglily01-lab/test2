# 超级大脑交易系统 — 策略总览

> 更新时间：2026-03-27

---

## 一、系统整体架构

```
Binance 实时价格 (WebSocket)
        │
        ▼
┌──────────────────────────────────────────────────────┐
│                  策略层（并行运行）                     │
│                                                      │
│  ① 主策略 V1信号评分       每5分钟扫描全交易对           │
│  ② BTC动量跟随策略         每分钟检测BTC价格变动         │
│  ③ 市场预测策略            每6小时分析+开单              │
│  ④ 15M突破策略             每15分钟扫描                  │
│  ⑤ U本位破位策略           每5分钟扫描，独立进程         │
│  ⑥ 币本位合约策略          独立进程，独立逻辑             │
└──────────────────────────────────────────────────────┘
        │
        ▼
┌──────────────────────────────────────────────────────┐
│                    平仓层                              │
│  SmartExitOptimizer                                  │
│  — 止损 / 止盈 / 最大持仓时间 / planned_close_time      │
└──────────────────────────────────────────────────────┘
        │
        ▼
  futures_positions       （模拟盘 account_id=2）
  live_futures_positions  （实盘，user_api_keys 中活跃账号）
```

---

## 二、U 本位策略一览

| 策略 | 文件 | 运行频率 | 保证金/杠杆 | SL / TP | source | 最大持仓 | 实盘同步 |
|------|------|---------|-----------|---------|--------|---------|---------|
| ① 主策略 V1信号评分 | smart_trader_service.py | 每5分钟 | 可配置 / 5x | 2% / 6% | SIGNAL_* | 无限制 | 是 |
| ② BTC动量跟随 | app/services/btc_momentum_trader.py | 每1分钟检测 | 模拟400U / 实盘100U × 5x | 2% / 6% | BTC_MOMENTUM | TOP50全开 | 是（需在TOP50内） |
| ③ 市场预测器 | app/services/market_predictor.py | 每6小时 | 100U × 5x | 2% / 6% | PREDICTOR | 5单 | 是（需在TOP50内） |
| ④ 15M突破 | app/services/breakout_15m_trader.py | 每15分钟 | 100U × 5x | 2% / 5% | 15M_BREAKOUT | 10单 | 是（需在TOP50内） |
| ⑤ U本位破位 | u_coin_style_trader_service.py | 每5分钟 | 400U(实盘100U) × 5x | 2% / 6% | U_COIN_STYLE | 20单 | 是（需在TOP50内，实盘<5单） |

---

## 三、策略详情

---

### 策略①：主策略 V1 信号评分

**文件**：`smart_trader_service.py`

**触发频率**：每5分钟扫描所有配置交易对（约300个）

**核心逻辑**：

```
K线数据 → 18个V1组件计分 → Big4动态阈值过滤 → V2协同确认 → K线回调建仓
```

#### 信号组件（18个，满分 LONG=244 / SHORT=270）

| 分类 | 组件 | LONG | SHORT | 说明 |
|------|------|------|-------|------|
| 位置(100H窗口) | position_low | +20 | — | 价格在100H区间底部30%以内 |
| 位置(100H窗口) | position_high | — | +20 | 价格在100H区间顶部70%以上 |
| 位置(100H窗口) | position_mid | +5 | +5 | 中间区间，顺量能方向加分 |
| 位置(24H窗口) | position_24h_low | +12 | — | 当日低位，更敏感，仿币本位 |
| 位置(24H窗口) | position_24h_high | — | +12 | 当日高位，Big4非强牛时触发 |
| 动量 | momentum_up_3pct | +15 | — | 24H涨幅 > 3% |
| 动量 | momentum_down_3pct | — | +15 | 24H跌幅 > 3% |
| 趋势 | trend_1h_bull | +20 | — | 24根1H中阳线≥15根 |
| 趋势 | trend_1h_bear | — | +20 | 24根1H中阴线≥14根 |
| 波动率 | volatility_high | +10 | +10 | 24H波幅>5%，顺主方向加分 |
| 连续K线 | consecutive_bull | +15 | — | 近10H阳线≥7根且涨幅<5% |
| 连续K线 | consecutive_bear | — | +15 | 近10H阴线≥7根且跌幅>-10% |
| 量能1.5×双框 | volume_power_bull | +25 | — | 1H+15M净力量≥2（阈值1.5×均量） |
| 量能1.5×双框 | volume_power_bear | — | +25 | 1H+15M净力量≤-2 |
| 量能1.5×单框 | volume_power_1h_bull | +15 | — | 仅1H净力量≥3 |
| 量能1.5×单框 | volume_power_1h_bear | — | +15 | 仅1H净力量≤-3 |
| 量能1.2×双框 | volume_power_12x_bull | +15 | — | 1H+15M净力量≥2（阈值1.2×，更敏感） |
| 量能1.2×双框 | volume_power_12x_bear | — | +15 | 1H+15M净力量≤-2（阈值1.2×） |
| 破位追空 | breakdown_short | — | +20 | 低位(<30%)+1H+15M双框强空 |
| Big4顺势 | big4_strong_bull_cont | +15 | — | Big4 STRONG_BULLISH + 自身涨>2% |

> 净力量 = 放量阳线数 - 放量阴线数（放量 = 成交量 > N × 24H均量）

#### 币本位风格补充层（2026-03-25 新增）

原始 U 本位使用 **100H 位置窗口** + **1.5× 量能阈值**，在震荡行情中不易触发信号。
币本位策略发现用 **24H 短窗口 + 1.2× 宽松阈值** 对当日走势更敏感，效果更好，因此将同套逻辑作为补充层移植到 U 本位：

| 新增组件 | 对应原有组件 | 区别 |
|---------|------------|------|
| position_24h_low | position_low | 位置窗口 24H（原 100H），对当日极值更敏感 |
| position_24h_high | position_high | 同上 |
| volume_power_12x_bull | volume_power_bull | 放量阈值 1.2×（原 1.5×），更易触发 |
| volume_power_12x_bear | volume_power_bear | 同上 |

两套组件并存：原有组件（100H/1.5×）保持不变，新组件作为额外加分项。
权重由 ScoringWeightOptimizer 每日自动调优，初始权重：position_24h ±12，volume_12x ±15。

#### 交易模式（2026-03-27 更新：并联运行）

主策略支持两种开仓模式，**同时运行，任一满足即开仓**：

**A. 信号确认模式（始终运行）**

基于信号分达到阈值触发，Big4 仅影响阈值高低：

| Big4状态（强度≥65） | LONG阈值 | SHORT阈值 |
|-------------------|---------|---------|
| BULLISH / STRONG_BULLISH | 60 | 60 |
| NEUTRAL | 65 | 60 |
| BEARISH / STRONG_BEARISH | 100（封死） | 60 |

**B. 趋势跟随模式（trading_mode=trend_following 时生效）**

捕捉 Big4 早期趋势窗口，信号分低但方向明确时也可入场：

| 条件 | 要求 |
|------|------|
| Big4 强度 | 20~50（早期趋势，非强势） |
| Big4 净方向 | bullish_count > bearish_count（多）或反之（空） |
| 币种信号分 | 20~50（中低分，非追高） |

> 两种模式取并集：信号确认 **OR** 趋势跟随，任一满足即可开仓。
> 横盘期（Big4 强度 <20）趋势跟随不触发，信号确认照常运作。

附加条件：
- 至少2个信号组合，方向一致（无反向信号）
- V2协同过滤（coin_kline_scores 表）：V2反向时需额外+25分才放行
- 信号黑名单检查（signal_blacklist 表）

#### 建仓方式（K线回调）

分3批建仓：30% → 30% → 40%，等K线回调确认再入场

#### 权重自动优化

每日 00:00 UTC，ScoringWeightOptimizer 根据近7日胜率自动调整各组件权重，范围 [5, 30]，每次幅度±2~3

---

### 策略②：BTC 动量跟随

**文件**：`app/services/btc_momentum_trader.py`

**触发频率**：每分钟检测 BTC 价格，内部4小时冷却

**核心思路**：BTC 短期急涨急跌带动全市场，跟随做 TOP50

#### 触发条件

监测4个时间窗口（15 / 30 / 45 / 60 分钟），**任意一个**满足：

```
|BTC涨跌幅| ≥ 1.5%  →  触发
涨幅 > 0  →  开 LONG
跌幅 < 0  →  开 SHORT
```

#### 开仓规则

1. 获取 `top_performing_symbols` TOP50 列表
2. 对每个交易对：
   - 已有同向仓 → 保留，跳过
   - 已有反向仓 → 先平仓，再开新仓
   - 无仓 → 直接开仓
3. 冷却：触发后 4 小时内不再触发

#### 参数

| 参数 | 模拟盘 | 实盘 |
|------|-------|------|
| 保证金 | 400U/单 | max_position_value（默认100U） |
| 杠杆 | 5x | max_leverage（默认5x） |
| 止损 | 2% | 2% |
| 止盈 | 6% | 6% |

#### 实盘同步

`live_trading_enabled=1` 时，对所有 `user_api_keys.status='active'` 账号调用 BinanceFuturesEngine 真实下单，成功后发 TG 推送。实盘开仓无需额外过滤（TOP50 已在获取列表时保证）

---

### 策略③：市场预测器

**文件**：`app/services/market_predictor.py`

**触发频率**：每6小时（app/main.py 调度，00:00 / 06:00 / 12:00 / 18:00 UTC）

**核心思路**：独立于信号评分体系，纯粹基于技术指标形态判断未来6小时方向

#### 分析逻辑

**数据**：72根1H K线 + 96根15M K线

**1H 指标（大方向）**：

| 指标 | 多头条件 |
|------|---------|
| EMA9 vs EMA26 | EMA9 > EMA26 |
| MACD(8/21/5) | 柱 > 0 且 ≥ 上一根（扩张中） |
| RSI(14) | 用于置信度 |
| ADX(14) | 用于趋势强度 |

**15M 指标（短期动能）**：

| 指标 | 多头条件 |
|------|---------|
| 近8根方向 | ≥5根上涨 |
| RSI趋势 | 近4根RSI持续上升 |
| 成交量 | 近4根 > 前4根均量×1.3 |

**方向判断（双周期共振）**：

```
1H 多头 AND 15M 多头  →  BULLISH
1H 空头 AND 15M 空头  →  BEARISH
其他                  →  NEUTRAL（不开单）
```

#### 置信度评分（0~100分）

| 条件 | 加分 |
|------|------|
| 1H+15M 方向一致（非NEUTRAL） | +30 |
| ADX > 30（强趋势） | +25 |
| ADX 20~30（弱趋势） | +10 |
| RSI < 40 或 > 60（动能明确） | +15 |
| MACD 柱同向扩大 | +15 |
| 成交量放大 > 1.3x | +15 |

#### 两套并行输出

**A. 虚拟回测单**（`prediction_backtest` 表，不限数量）
- confidence ≥ 40，每个交易对独立开，仅统计预测精度，不影响实际资金

**B. 真实模拟单**（`futures_positions`，最多5单）
- confidence ≥ 70，source = 'PREDICTOR'
- 保证金100U × 5x，SL 2%，TP 6%
- 开仓时写入 `planned_close_time = NOW() + 6H`，6小时后自动平仓

#### 平仓机制

1. **到期平仓**：`planned_close_time` 到期 → SmartExitOptimizer 执行（同步平实盘）
2. **保底清理**：每次运行时额外平掉持仓 > 5.5H 的 PREDICTOR 单
3. **止损/止盈**：SmartExitOptimizer 监控

#### 实盘同步

`live_trading_enabled=1` 时，confidence ≥ 70 的单同步到实盘（上限同为5单），成功后发 TG 推送。**实盘还需通过 TOP50 过滤**：不在 `top_performing_symbols` 内的交易对仅开模拟单，不同步实盘

---

### 策略④：15M 突破策略

**文件**：`app/services/breakout_15m_trader.py`

**触发频率**：每15分钟扫描全部配置交易对

**核心思路**：捕捉震荡市中 15M 级别的量价突破信号，趋势信号未反转时提前入场

#### 三个条件同时满足才入场

| 条件 | 做多 | 做空 |
|------|------|------|
| 价格突破 | 当前价 > 前8根15M最高价 | 当前价 < 前8根15M最低价 |
| RSI 拐头 | RSI(14)>50 且较4根前上升≥3点 | RSI(14)<50 且较4根前下降≥3点 |
| 量能放大 | 最新1根量 > 前8根均量 × 1.5 | 同上 |

#### 参数

| 参数 | 值 |
|------|-----|
| 保证金 | 100U/单 |
| 杠杆 | 5x |
| 止损 | 2% |
| 止盈 | 5% |
| 同币种同向冷却 | 4小时 |
| 最大持仓 | 10单 |

#### 实盘同步

`live_trading_enabled=1` 时，开仓后调用 BinanceFuturesEngine 同步实盘（绑定 paper_position_id），成功后发 TG 推送。**实盘还需通过 TOP50 过滤**：不在 `top_performing_symbols` 内的交易对仅开模拟单，不同步实盘

---

### 策略⑤：U 本位破位策略

**文件**：`u_coin_style_trader_service.py`

**开关**：`system_settings.u_coin_style_enabled`

**触发频率**：每5分钟扫描，独立进程运行

**核心思路**：独立于主策略的 Big4 破位体系，识别趋势破位加速信号

#### 参数

| 参数 | 值 |
|------|-----|
| 保证金（模拟） | 400U/单 |
| 保证金（实盘） | 100U/单 |
| 杠杆 | 5x |
| 止损 | 2% |
| 止盈 | 6% |
| 最大持仓（模拟） | 20单 |
| 实盘额外限制 | 当前实盘持仓 < 5单才开 |
| source 标记 | U_COIN_STYLE |

#### 信号权重

独立权重表：`signal_scoring_weights WHERE strategy_type='u_coin_style'`，初始14个组件，与主策略不共享

#### 开仓条件

1. 信号评分 ≥ 60（独立阈值）
2. BreakoutSystem 破位验证通过
3. 3小时内平过该品种则跳过
4. 实盘：`live_trading_enabled=1` 且在 TOP50 内 且实盘持仓 < 5

#### 熔断条件

| 触发 | 条件 |
|------|------|
| 亏损熔断 | 过去3小时亏损 > 300U → 自动停止当日交易 |

#### 持仓池共享

与主策略（smart_trader）、BTC动量、预测器**共享** `futures_positions` 持仓池（account_id=2），互相防重复开仓、防对冲。

---

### 策略⑥：币本位合约策略

**文件**：`coin_futures_trader_service.py`

**开关**：`system_settings.coin_futures_trading_enabled`

**账户**：account_id = 3，交易对格式 BTC/USD

**逻辑**：独立评分体系（9个组件，与U本位不共享权重），详见 [币本位超级大脑逻辑梳理.md](币本位超级大脑逻辑梳理.md)

| 参数对比 | U本位主策略 | 币本位 |
|---------|-----------|-------|
| 止损 | 2% | 3% |
| 止盈 | 6% | 2% |
| 位置窗口 | 100H | 24H |
| 量能阈值 | 1.5× | 1.2× |

---

## 四、策略适用场景对比

| 策略 | 最适合 | 最弱场景 |
|------|--------|---------|
| 主策略 V1 | 中长趋势明确，Big4 单边 | 震荡横盘（评分积累慢） |
| BTC 动量 | BTC 急速单边行情（振幅≥1.5%） | 宽幅震荡（反复触发冷却） |
| 预测器 | 趋势启动初期 | 高波动快速反转 |
| 15M 突破 | 震荡后放量突破 | 趋势市（易假突破频繁触发） |
| U 本位破位 | Big4 破位加速初段 | 无方向震荡 |
| 币本位 | 短线震荡波动 | 长周期趋势（止盈2%易被扫损） |

五个U本位策略互补：趋势市靠主策略+BTC动量+破位，震荡市靠预测器+15M突破

---

## 五、平仓层：SmartExitOptimizer

**文件**：`app/services/smart_exit_optimizer.py`

统一管理所有策略的模拟盘持仓，触发条件：

| 触发条件 | 说明 |
|---------|------|
| 止损 | mark_price 触及 stop_loss_price |
| 止盈 | mark_price 触及 take_profit_price |
| 最大持仓时间 | 超过 max_hold_hours（system_settings） |
| 计划平仓时间 | 超过 planned_close_time（预测器专用） |

平仓时同步检查 `live_futures_positions.paper_position_id`，有对应实盘单则同步调用交易引擎平仓

---

## 六、自学习优化循环

```
交易结果 (futures_positions.realized_pnl)
    │
    ▼
daily_review_report.py（每日 00:00 UTC）
    │
    ├── 发 Telegram 复盘报告（4条）
    │
    └── ScoringWeightOptimizer.adjust_weights()
            │
            ├── 读取近7天已平仓记录
            ├── 按 Big4 市场状态过滤（熊市多头亏损不污染多头权重）
            ├── 计算各组件胜率 + 平均盈亏
            └── 调整 signal_scoring_weights 表
                    权重范围 [5, 30]，每次 ±2~3
```

---

## 七、系统开关（system_settings 表）

| 设置键 | 作用 |
|--------|------|
| `live_trading_enabled` | 实盘开关（同时控制TOP50过滤） |
| `u_futures_trading_enabled` | U本位主策略总开关 |
| `coin_futures_trading_enabled` | 币本位总开关 |
| `u_coin_style_enabled` | U本位破位策略开关 |
| `btc_momentum_enabled` | BTC动量跟随开关 |
| `predictor_enabled` | 市场预测器开关 |
| `breakout_15m_enabled` | 15M突破策略开关 |
| `allow_long` | 全局允许做多 |
| `allow_short` | 全局允许做空 |
| `big4_filter_enabled` | Big4过滤器开关 |
| `max_hold_hours` | 最大持仓小时数（预测器/BTC动量读取此值） |
| `trading_mode` | 主策略开仓模式：`signal_confirmation` / `trend_following` |

---

## 八、实盘质量控制

### TOP50 过滤规则（2026-03-25 启用）

所有实盘开仓必须通过 `top_performing_symbols` TOP50 列表过滤，基于历史 realized_pnl 排名：

| 策略 | 模拟盘 | 实盘 |
|------|-------|------|
| 主策略 V1 | 无限制（全300对） | 无限制（全300对） |
| BTC动量跟随 | 直接使用TOP50列表 | TOP50列表即实盘开仓范围 |
| 市场预测器 | 无限制 | 需在TOP50内才同步实盘 |
| 15M突破 | 无限制 | 需在TOP50内才同步实盘 |

TOP50 列表每日 00:00 UTC 由 `update_top_performers.py` 刷新，从已平仓记录中按 `total_realized_pnl` 降序取前50名（至少5笔交易才纳入统计）

### Level3 永久禁止名单

`trading_symbol_rating.rating_level=3` 的交易对所有策略均跳过：

| 标记原因 | 代表交易对 |
|---------|-----------|
| 流动性差/波动极端 | AIN/USDT、ARIA/USDT、FHE/USDT |
| 多次亏损 | FET/USDT |
| 新增（2026-03-25） | ALLO/USDT、1000CHEEMS/USDT |

---

## 十、调度频率汇总

| 任务 | 频率 | 文件 |
|------|------|------|
| BTC价格检测（动量触发） | 每1分钟 | btc_momentum_trader.py（内嵌 smart_trader_service） |
| 评分更新（存储过程） | 每5分钟 | update_all_coin_scores |
| 主策略信号扫描 | 每5分钟 | smart_trader_service.py |
| U本位破位扫描 | 每5分钟 | u_coin_style_trader_service.py（独立进程） |
| 采集服务守护 | 每5分钟 | fast_collector_service.py watchdog |
| 技术信号缓存更新 | 每15分钟 | update_technical_signals_cache |
| 15M突破扫描 | 每15分钟 | breakout_15m_trader.py |
| 市场预测分析 | 每6小时 | market_predictor.py（手动触发：python scripts/run_predictor.py） |
| 每日复盘+权重优化 | 每天 00:00 UTC | daily_review_report.py |

---

## 十一、数据流简图

```
K线采集（每分钟）
    └→ kline_data 表
            │
            ├→ coin_kline_scores（每5分钟评分）
            │       └→ 主策略① 读取V1+V2信号
            │
            ├→ btc_momentum_trader（每分钟）
            │       └→ BTC动量策略②
            │
            ├→ market_predictor（每6小时）
            │       └→ 预测策略③ → prediction_backtest + futures_positions
            │
            └→ breakout_15m_trader（每15分钟）
                    └→ 15M突破策略④

开仓记录
    ├→ futures_positions（模拟盘 account_id=2）
    └→ live_futures_positions（实盘，paper_position_id 关联模拟单）

平仓监控
    └→ SmartExitOptimizer（持续运行，平仓时同步实盘）

每日自优化
    └→ daily_review_report.py → signal_scoring_weights
```

---

*文档最后更新：2026-03-27*
