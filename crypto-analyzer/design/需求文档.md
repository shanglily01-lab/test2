# 超级大脑量化交易系统 - 需求文档

> **⚠️ 本文档 v3.6 已过时（2026-05-28）。**  
> **生产业务逻辑唯一权威来源：`docs/REQUIREMENTS_LOGIC_ZH.md`（v4.0，2026-06-18）。**  
> 改代码必须同步该文档 §0 维护规范。下文仅供历史参考。

**版本**: v3.6
**日期**: 2026-05-28
**状态**: 正式版 (基于实际代码校正)
**修订**: v3.5 → v3.6 DeepSeek 探索/预测 + 操作手册 + Unknown 信号修复 + 清理无效策略 (7项改进),见末尾"v3.6 修订记录"

---

## 1. 项目背景与目标

### 1.1 背景

加密货币市场 7×24 小时不间断运行,价格波动剧烈,人工监盘效率低且容易受情绪影响。本系统旨在构建一套全自动量化交易平台,基于技术信号、市场趋势和智能风控,在 Binance 交易所自动执行合约交易。

### 1.2 目标

- 实现 U 本位合约、币本位合约的全自动信号扫描与开平仓
- 提供 Web 管理界面,支持实时监控、参数调整与回顾分析
- 通过信号评分、Big4 趋势过滤、Top50 筛选等多道关卡控制风险
- 支持模拟盘与实盘切换,降低实盘测试风险
- 通过每日自动复盘与权重优化,持续改进策略表现

### 1.3 范围

| 模块 | 包含 | 不包含 |
|------|------|--------|
| 交易所 | Binance 合约 | 其他交易所、现货 |
| 合约类型 | U本位合约、币本位合约 | 期权 |
| 自动化 | 信号检测、开平仓、风控、复盘 | 人工干预执行 |
| 数据来源 | Binance K线、Farside ETF、BitcoinTreasuries | 链上数据实时监控 |

---

## 2. 用户角色

| 角色 | 描述 | 主要操作 |
|------|------|---------|
| 管理员 | 系统所有者,全权控制 | 配置策略参数、管理API密钥、查看所有数据 |
| 交易员 | 使用系统进行交易的用户 | 监控持仓、手动干预、查看报告 |

---

## 3. 功能需求

### 3.1 自动交易引擎

#### 3.1.1 U 本位合约主策略 (smart_trader_service.py)

**FR-U01** 信号扫描
- 每 5 分钟扫描白名单交易对 (SmartDecisionBrain.scan_all)
- 对每个交易对计算多空评分 (LONG 满分 217 / SHORT 满分 243)
- 根据 Big4 市场趋势决定开仓阈值
- **实际写入 `futures_positions.source = 'smart_trader'`** (代码预留 signal_confirm / trend_follow 但未启用)

**FR-U02** 开仓阈值 (Big4 过滤器开启时, v3.4 放宽)
- STRONG_BULLISH：LONG=60+从众惩罚, SHORT=60
- STRONG_BEARISH：LONG=100 (封死), SHORT=60
- BULLISH (signal_strength ≥ 50)：LONG=60+从众惩罚+5 (即65), SHORT=60
- BEARISH (signal_strength ≥ 50)：LONG=100 (封死), SHORT=60
- NEUTRAL：LONG=65+15 (即80,且要求 score≥80), SHORT=60
- max_threshold=150：分数超过 150 拒绝 (防追涨杀跌)
- 实盘模式：交易对必须在 top_performing_symbols Top50 内

**FR-U03** 风控关卡 (16 道 + 3项优化, v3.4 扩展)
- 持仓数量限制 (MAX_POSITIONS=50,可通过 system_settings.max_positions 调整)
- 同币种同方向防重复
- 信号黑名单 (signal_blacklist)
- 评级黑名单 (rating_level >= 3 禁止)
- **资金费率动态阈值 (v3.4: 24h均值±2σ替代固定±0.1%)** → [FUNDING_VETO]
- 反向信号过多 (≥3 个拒绝)
- Big4 方向封锁
- **信号拥挤度惩罚 (v3.4: 同向≥4个组件时扣溢出分)** → [CROWDING]
- 从众探测器 (≥80% 同向时阈值 +10) → [HERDING]
- **V2数据过期保守模式 (v3.4: 数据>15min时阈值+10)**
- 连续亏损习惯化 (最多 5 次后阈值×0.6→相当于提升至 100 分) → [HABITUATION]
- 不应期冷却 (同币同方向 120 分钟内不重复) → [REFRACTORY]
- 信号衰减检测 (亏损时若评分反转则平仓) → [SIGNAL_DECAY]
- 盈利熔断 (6 小时盈利 > 1000U 自动暂停) → [PROFIT_GUARD]
- 亏损熔断 (3 小时亏损 > 300U 自动暂停) → [LOSS_GUARD]
- 震荡市检测 (is_choppy=True 非强信号时暂停) → [CHOPPY_MARKET]

**FR-U04** 建仓执行 (v3.4 增强)
- 使用 SmartEntryExecutor (价格采样 V1,15 分钟采样,一次性建仓)
- 杠杆：LEVERAGE=5
- 保证金：Level 0 = 400U,Level 1 = 100U,Level 2 = 50U,Level 3 = 不开仓
- **差异化仓位大小 (v3.4): 按评分动态调整,弱信号x0.6,强信号x1.2**

**FR-U05b** 模拟盘限价开仓（`paper_limit_entry.py`）[2026-06-21]
- **中线四策略**: 强制限价；偏移 **做多−3%/做空+3%**；6h 超时；不受 `paper_limit_entry_enabled` 关闭影响
- **探索/预测/主策略等**: 偏移读 `system_settings.paper_limit_long/short_offset_pct`（默认 0.5%，范围 0.1~1%）
- 成交: `futures_limit_order_executor` 每 5s 用 **ticker** 价触价（与 UI 一致）；超时 `expire` 或 `convert_market`
- 成交后 PaperSync（§4 REQ-LIVE）；立即同步 `sync_filled_order_now`

**FR-U05c** AI / 中线平仓监控（`position_sl_tp_monitor.py`，main 进程）[2026-06-21]
- AI 探索/预测/战术: **硬 SL/TP** + **ai-trail-tp**（peak≥3% 价格收益，回撤≥1%）；**中线不含 ai-trail-tp**
- **中线**: **排除** SmartExitOptimizer；planned_close 15 天 + 爆仓
- 探索/预测: planned_close 4h

**FR-U05d** 模拟仓持仓顾问 [2026-06-21]
- scheduler 每 **15min** tick；Gemini 监管 `gemini_explore/predict`；DeepSeek 监管其余（**不含中线**）
- 每仓 **15min** 复审；上次审核浮盈现已亏损 → urgent 立即再审
- 盈利 sell 门槛 ROI **+8%** + 15m 反向≥4；过早 sell 程序化严格拦截
- 盈利 ROI≥**+8%** 且 15m 明确转弱（反向≥4）→ 倾向 observe/sell；过早 sell 程序化严格拦截
- sell 关模拟仓；`live_close_enabled=1` 时同步平映射实盘

**FR-U05** 平仓管理 (SmartExitOptimizer) [v3.4 多重增强]
- 止损/止盈：**实际从 system_settings 读取,当前值 stop_loss_pct=0.02 / take_profit_pct=0.03**
  - **v3.4 新增动态SL/TP**: 按信号评分自动调整,弱信号SL放宽至5%/TP缩至6%,强信号SL缩至2%/TP放至10%
  - 代码默认 SL 2% / TP 5% (BTC_MOMENTUM 与 PREDICTOR 内部默认)
  - 主策略 SL 2% / TP 6% (config.yaml 写)
  - **运行时以 system_settings 为准**,管理员可随时改
- **v3.4 退出原因枚举**: 每种平仓reason映射到短码(SL/TP/TREND_REV/TRAILING_TP等),写入 notes 和 close_reason
- **v3.4 监控频率递减**: <30min:1s/次, 30min-2h:3s/次, 2h-4h:5s/次, >4h:10s/次
- 最大持仓时间：从 system_settings.max_hold_hours 读取,**当前实测值 3 小时**
- ROI ≤ -10% 极端亏损兜底立即平仓
- ROI ≥ 25% 超高盈利立即全部平仓
- 趋势反转检测：60 分钟最小持仓后,LONG ≥11/16 / SHORT ≥9/16 根 5M K 线逆向 → 平仓
- 多策略 S1-S7 持仓**跳过**动态趋势反转、K 线衰减检测,只用固定 SL/TP + planned_close_time
- 智能平仓窗口：planned_close_time 前 30 分钟启动 PriceSampler 采样最优价
- 移动止盈：**代码注释为"暂时关闭"** (0.5% 阈值被插针频繁触发)

#### 3.1.2 币本位合约交易 (coin_futures_trader_service.py)

**FR-C01** 信号扫描
- 每 5 分钟扫描币本位合约交易对
- 使用 CoinFuturesDecisionBrain 决策逻辑

**FR-C02** 开仓条件
- 基础阈值：threshold=60
- Big4 偏空市场时 LONG 阈值动态上调至 68
- Big4 = NEUTRAL：只过滤 LONG,SHORT 仍可执行 (2026-04-05 修复)

**FR-C03** 特色功能
- detect_big4_bottom_reversal：Big4 触底反转检测,触发后允许 LONG 跨过封锁
- detect_big4_top_reversal：Big4 见顶反转检测,触发后允许 SHORT 跨过封锁
- check_breakout：BreakoutSystem 破位系统集成
- 紧急干预 (emergency_intervention 表) block_long / block_short 强制控制方向

**FR-C04** 建仓执行
- 杠杆：LEVERAGE=5,MAX_POSITIONS 不限 (999)
- 保证金：正常 400U,黑名单 100U
- 盈亏熔断阈值低于 U本位 (盈利 200U/6H,亏损 100U/3H)

#### 3.1.3 BTC 动量跟随策略 (app/services/btc_momentum_trader.py)

**FR-B01** 触发条件
- 15/30/45/60 分钟窗口内 BTC 涨跌 ≥ 1.5% 触发
- 4 小时冷却 (_last_trigger_time,重启后清零)

**FR-B02** 开仓逻辑
- 在 Top50 全部交易对同向跟随 BTC
- 同向持仓保留,反向先平后开
- 模拟盘 400U×5,实盘每 API 账号 max_position_value (默认 100U) × max_leverage (默认 5)
- 每实盘账号最多 20 个 OPEN 仓位

**FR-B03** 价格数据 (2026-05-14 修复)
- 1m K 线已于 2026-01-22 停采,改用 5m K 线
- _preload_btc_history:启动预填充 90 分钟 5m K 线 (18 根)
- _get_symbol_price: WS 优先,fallback 5m K 线,**数据超过 15 分钟旧拒绝使用**

**FR-B04** 持仓管理
- 默认 SL 2% / TP 5% (实际读 system_settings)
- planned_close_time = open_time + max_hold_hours
- 由 SmartExitOptimizer 执行平仓

#### 3.1.4 市场预测器 (app/services/market_predictor.py)

**FR-P01** 调度
- 每 6 小时由 app/main.py 调度运行一次

**FR-P02** 虚拟单 (prediction_backtest 表)
- confidence ≥ 40 对所有交易对开虚拟单
- 100U × 5x 杠杆,5.5 小时后自动结算 PnL
- 仅记录用于回测,不实际下单

**FR-P03** 真实模拟单 (futures_positions, source='PREDICTOR')
- confidence ≥ 70 全部开单,**代码无数量上限** (v3.0 文档错记为 5)
- 默认 400U × 5x;评级 Level 1 → 100U;Level 2 → 50U
- 默认 SL 2% / TP 5% (实际读 system_settings)
- planned_close_time = open_time + max_hold_hours

**FR-P04** 实盘同步
- live_trading_enabled=1 且 symbol 在 Top50 时,通过 _sync_live() 调用 BinanceFuturesEngine.open_position()
- 模拟单平仓时同步通过 close_position_by_symbol 关闭实盘
- **历史数据:实盘 49 单 -131U,模拟 1217 单 +899U,实盘期望显著差于模拟**

#### 3.1.5 多策略服务 (app/services/multi_strategy_service.py) [v3.1 新增, v3.2 扩展 S8/S9, v3.3 取消 run_fast]

调度方式:内嵌在 smart_trader_service 主循环 (5 min/轮),
- ~~`run_fast()`：每 5 分钟 → S2 + S4 + S7~~ **[2026-05-17 commit 5704f0f 取消]**
  - 原因: 5月数据 S1-S9 一笔实际开仓都没,但 run_fast 每 5min 跑空扫描浪费 CPU
- `run_slow()`：每 30 分钟一轮 → S1 + S2 + S3 + S4 + S5 + S6 + S7 + S8 + S9
  - 内部 `_last_slow_scan` 限速 30 min
  - S9 在 run_slow 内另加 6h 限速 (Gemini API 收费)

**FR-M01** 共同约束 (2026-05-17 v3.3 统一)
- account_id=2 (与主策略共享持仓池)
- 任一策略开仓前 `_has_multi_strategy_position(symbol)` 检查,**同 symbol 在 S1-S9 任意已有 open 则跳过**
- 任一策略 `_get_current_price` WS 不可用时直接拒绝开仓 (宁可错过)
- 杠杆**统一 5x** (2026-05-17 commit 28ffa2f, S2 从 10x 改为 5x)
- 模拟盘保证金**统一 500U** (原 200-300U,统一后简化风控)
- 实盘保证金由 `user_api_keys.max_position_value` 控制 (每 API key 独立)
- **SL / TP / 持仓时间从 `system_settings` 动态读** (`stop_loss_pct` / `take_profit_pct` / `max_hold_hours`),不再硬编码;UPDATE 后约 60 秒内生效
- live_trading_enabled=1 时同步实盘,**实盘需通过 TOP50 过滤** (`_is_symbol_in_top50`,2026-05-17 v3.3 新增,与主策略一致);**模拟盘不卡 TOP50**

**FR-M02** S1-S9 策略入场条件汇总

| 策略 | source | 方向 | MAX | Big4 过滤 | Kill switch | 入场条件 (摘要) |
|------|--------|-----|-----|----------|------------|----------------|
| S1 早期做多 | s1_early_long | LONG | **999\*** | 跳过 BEARISH / STRONG_BEARISH | 无 (默认 ON) | 1H_RSI 25-52 连升 2 根 + 4H_MACD hist 连升 2 根且前根 < 0.002 + 日价 / MA20 ∈ [80%, 105%] + 当日量 ≥ 7日均量 × 0.9 |
| S2 无量回调 | s2_pullback_long | LONG | **999\*** | 跳过 BEARISH | 无 | 48H 价格振幅 ≥ 12% + 现价从 48H 高回落 15-38% + 15m_RSI 30-58 且最近 2 根上升 |
| S3 顶部做空 | s3_top_short | SHORT | **999\*** | 跳过 STRONG_BULLISH | 无 | 1H_RSI ≥ 70 顶背离 (当前不是最近 6 根最高) + 现价从 48H 高回落 3-15% + 48H 涨幅 > 20% + 4H_MACD hist 下行 + 近 3 根 1H 至少 2 阴 |
| S4 反弹衰竭 | s4_rebound_short | SHORT | **999\*** | 无 | 无 | 14 日 1H 高点 50-85% 反弹 + 曾跌 ≥ 15% + 现价 > 7 日低 × 1.05 + MACD↓ / RSI<65↓ / 上涨 K 均量 < 下跌 K 均量 **三选二** |
| S5 大币超卖 | s5_large_oversold | LONG | 3 | 跳过 STRONG_BEARISH | 无 | 仅 [BTC,ETH,SOL,BNB,XRP] + 4H_RSI < 32 但比前根高 + 现价 < 日 MA20 |
| S6 量能异动 | s6_vol_spike | LONG | 5 | 跳过 BEARISH | 无 | 排除大市值 + FIO + 12H 量峰 > 3.5× 48H 均量 + 当前量比 1.2-5× + 1H_RSI 28-55 + 价格 / 1H_MA20 ∈ [75%, 108%] + 3H 涨幅 < 5% |
| S7 均线支撑 | s7_ma_support | LONG | 5 | 跳过 BEARISH | 无 | 排除大市值 + 现价 / 20H_MA ∈ [82%, 95%] + 当前 1H 阳线且收盘 > 前根 + 当前量 ≥ 10根均量 × 1.2 |
| S8 顶部反转 [v3.2] | s8_topshort | SHORT | 3 | 跳过 STRONG_BULLISH | `s8_topshort_enabled` **默认 OFF** | 上市 ≥ 12 天 + 48H 内出现 pump ≥ 80% + pump 后 6 根 1H 无新高 + 信号年龄 ≤ 6H + 现价 > pump 起点低 + 从 pump 峰回落 < 50% + 24H 跌幅 > -15% + 3H 15m K 线区间位置 ≥ 10% |
| S9 Gemini AI [v3.2] | s9_gemini_ai | LONG only | 5 | 无 | `s9_gemini_ai_enabled` **默认 OFF** | 每 6h 跑一轮,取 24H 成交额 top30 (排除 BTC/ETH/BNB/SOL/XRP + 证券类) + 调 Google Gemini API + Gemini 返回 direction=long 且 expected_pnl_pct ≥ 1% |

**\* MAX=999** 是测试阶段标记 (代码注释明确"上线后改回 3")。**开实盘前必须改回**,否则单策略可能无限开仓。

**FR-M03** S1-S9 各策略详细入场算法

##### S1 早期做多 (`scan_s1_early_long`)
1. 持仓数 < S1_MAX_POSITIONS (=999)
2. Big4 不在 (BEARISH, STRONG_BEARISH)
3. 候选池: `_get_candidate_symbols(min_abs_change=3.0)` 即 24H 涨跌幅绝对值 ≥ 3%
4. 同 symbol 在 S1-S9 任意已开仓则跳过
5. 1H K 线 (30 根)
   - RSI 最新值 ∈ [25, 52]
   - RSI 上一根 < 最新值 (上升)
6. 4H K 线 (40 根),MACD histogram (12,26,9)
   - 最新 3 根 hist 严格递增 (h1 < h2 < h3)
   - h1 (倒数第 3 根) < 0.002 (确认仍在低位)
7. 日 K 线 (25 根)
   - MA20 计算
   - 日收 ∈ [MA20 × 80%, MA20 × 105%]
   - 日 volume ≥ 近 7 日均量 × 0.9
8. 全部满足 → 开 LONG;reason 含 RSI / MACD / 价/MA20 / 量比

##### S2 无量回调做多 (`scan_s2_pullback_long`)
1. 持仓数 < S2_MAX_POSITIONS (=999)
2. Big4 不在 (BEARISH, STRONG_BEARISH)
3. 候选池: `min_abs_change=8.0`
4. 1H K 线 (52 根)
   - 48 根内 (high-low)/low ≥ 12% (有足够振幅)
   - (48 根 high - 当前 close) / 48 根 high ∈ [15%, 38%] (从高点回落)
5. 15m K 线 (30 根)
   - RSI ∈ [30, 58]
   - RSI 最新 > 上一根 (企稳上升)
6. 全部满足 → 开 LONG

##### S3 顶部做空 (`scan_s3_top_short`)
1. 持仓数 < S3_MAX_POSITIONS (=999)
2. Big4 ≠ STRONG_BULLISH
3. 候选池: `min_abs_change=15.0` (大涨幅币种)
4. 1H K 线 (52 根)
   - RSI 最新 > 70
   - RSI 顶背离: 当前 RSI < 最近 6 根 RSI 最高 (说明 RSI 已开始下行但价格还在涨)
   - 现价从 48H high 回落 ∈ [3%, 15%]
   - 48H 涨幅 = (48H high - 48 根前收) / 48 根前收 > 20%
5. 4H K 线 (40 根) MACD hist 下行 (h3<h2 或 h2<h1)
6. 最近 3 根 1H K 线 ≥ 2 根阴线 (close < open)
7. 全部满足 → 开 SHORT

##### S4 反弹动能衰竭做空 (`scan_s4_rebound_short`)
1. 持仓数 < S4_MAX_POSITIONS (=999)
2. **无 Big4 过滤** (反弹是市场结构信号,与 Big4 无关)
3. 候选池: `min_abs_change=8.0`
4. 14 日 1H K 线 (14×24+2 = 338 根)
   - two_week_high = 14 日 high 最大值
   - 当前价 / two_week_high ∈ [50%, 85%]
   - 7 日 low 计算; (two_week_high - 7日low) / two_week_high ≥ 15% (有实质性回落)
   - 现价 > 7日low × 1.05 (从低点已反弹 ≥ 5%)
5. 最近 52 根 K 线计算指标,三个空头条件:
   - 条件 A — MACD hist 下行: h3<h2 或 h2<h1
   - 条件 B — RSI < 65 且 RSI 下行
   - 条件 C — 量萎缩: 近 10 根中,上涨 K 均量 < 下跌 K 均量
   - **A/B/C 三选二**
6. 满足 → 开 SHORT

##### S5 大币 4H 超卖反弹做多 (`scan_s5_large_oversold`)
1. 持仓数 < 3
2. Big4 ≠ STRONG_BEARISH
3. **白名单**: [BTC, ETH, SOL, BNB, XRP]
4. 4H K 线 (40 根)
   - RSI < 32 (超卖)
   - RSI 当前 > 上一根 (回升,不再下落)
5. 日 K 线 (25 根)
   - 当前日收 < MA20 (均值回归空间)
6. 满足 → 开 LONG

##### S6 量能异动做多 (`scan_s6_vol_spike`)
1. 持仓数 < 5
2. Big4 不在 (BEARISH, STRONG_BEARISH)
3. **排除大市值 17 个 + FIO/USDT** (`S6_EXCLUDE_SYMBOLS`)
   - 大币量能信号失真;FIO 回测 10 天 8% 胜率
4. 1H K 线 (50 根)
   - 48 根均量 (vol_base)
   - 当前量 / vol_base ∈ [1.2, 5.0]
   - 12 根 high vol / vol_base > 3.5 (近 12H 出现量能爆发)
   - RSI ∈ [28, 55]
   - 现价 / 1H_MA20 ∈ [75%, 108%]
   - 过去 3 根 1H 涨幅 < 5% (未追高)
5. 满足 → 开 LONG

##### S7 小币均线支撑反弹做多 (`scan_s7_ma_support`)
1. 持仓数 < 5
2. Big4 不在 (BEARISH, STRONG_BEARISH)
3. 排除大市值 (同 S6 列表)
4. 1H K 线 (30 根)
   - 20H 简单均线 (最后 21 根 close mean)
   - 现价 / 20H_MA ∈ [82%, 95%] (在均线下方但未深跌)
   - 当前 K 线: close > open 且 close > 前根 close (阳线 + 高于前收)
   - 当前量 ≥ 10 根均量 × 1.2 (量能确认)
5. 满足 → 开 LONG

##### S8 顶部反转做空 [v3.2] (`scan_s8_topshort`)
1. **kill switch**: `s8_topshort_enabled` 必须 ON (默认 OFF)
2. 持仓数 < 3
3. Big4 ≠ STRONG_BULLISH
4. 候选池: `min_abs_change=50.0` (大涨幅币种)
5. 上市 ≥ 12 天 (`_s8_has_min_listed_history`,15 分钟缓存)
6. 1H K 线 (LOOKBACK 48 + NO_NEW_H 6 + 20 = 74 根)
   - 倒序找最近一个满足 pump 的 i:
     - pump_i = (highs[i] - min(lows[i-48:i])) / min ≥ 80%
     - 之后 6 根 1H 全部 high < highs[i] (无新高)
     - i + 6 的 K 线 open_time 距今 < 6H (信号未老化)
   - 现价 > pump 起点低点 (信号未失效)
   - (峰值 - 现价) / 峰值 < 50% (跌得不太狠,不接刀)
7. `price_stats_24h.change_24h` > -15% (24H 未崩盘)
8. `_s8_entry_position_check`: 3H 内 15m K 线 (12 根) 计算区间位置 (cur - lo) / (hi - lo) × 100 ≥ 10% (现价不在低点)
9. 满足 → 开 SHORT

##### S9 Gemini AI 抄底反转做多 [v3.2] (`scan_s9_gemini_ai`)
1. **kill switch**: `s9_gemini_ai_enabled` 必须 ON (默认 OFF,因 Gemini API 收费)
2. 距上次 `_last_s9_run` < 6h 直接 return (内部限速)
3. 持仓数 < 5
4. Gemini client 初始化 (失败 return,不影响其他策略)
5. 候选池 `_s9_get_top30_symbols`:
   - 24H 成交额 ≥ 1000 万 USDT
   - 按成交额降序取前 100,排除 BTC/ETH/BNB/SOL/XRP + 证券类 (`is_security`),取前 30
6. 对每个 symbol:
   - 准备数据: 15 日日 K + 7 日 1H + 8H 15m + RSI(14) (1d/1h/15m) + 7 日高低 / 区间位置
   - 构造 prompt: 抄底反转 LONG-only,要求 distance_above_7d_low ≤ 8% / 量缩 + 长下影 / RSI 超卖 / 不在自由落体
   - 调 `gemini-3-flash-preview` (默认),JSON 解析
   - direction='short' 自动降级为 'skip' (S9 是 LONG-only)
   - direction='skip' 跳过;direction='long' 且 expected_pnl_pct ≥ 1% → 开 LONG
   - 每 symbol 间 sleep 1.0s 防 rate limit
7. 一轮结束记录 opened/skipped/errs

**FR-M04** S1/S3 历史无止损配置已废弃

旧版 (v3.1) 文档记录"S1/S3 入场后无止损,只有止盈"。**v3.3 起所有 S1-S9 全部走 system_settings 动态 SL/TP**,不再有"无止损"策略。SmartExitOptimizer 仍兜底 (P5 极端止损 ROI ≤ -15% + 30min 豁免)。

**FR-M05** S8/S9 集成历史

- 原 `strategy_live.py` (顶部反转做空, 1261 行) 和 `strategy_bigmid.py` (Gemini AI, 1002 行) 来自 test3 系统
- 2026-05-15 commit 75282b0: 从 test3 迁入 test2 作为独立进程跑
- 2026-05-16 commit c351f62: 重构成 S8/S9 集成进 multi_strategy_service
- 2026-05-16 commit d52eb5a: 加 kill switch `s8_topshort_enabled` / `s9_gemini_ai_enabled` 默认 OFF
- 旧文件 `strategy_live.py` + `strategy_bigmid.py` 保留 DEPRECATED 头部作为 rollback fallback
- S9 需要安装 `google-genai` Python 包 + `.env` 配 `GEMINI_API_KEY` / `GEMINI_MODEL`

**S8 vs 原 strategy_live 行为差异**:
- 改用市价单 (原用限价 + 七上八下 4h_high*0.8)
- 平仓由 SmartExitOptimizer 统一接管 (原有 trail/early-sl/breakeven 不再使用)
- 增加 Big4 STRONG_BULLISH 过滤
- 与 S1-S7 共享持仓池,_has_multi_strategy_position 去重
- SL/TP 由 system_settings 动态控制 (原硬编码 20% / 12%)

**S9 vs 原 strategy_bigmid 行为差异**:
- 改用市价单 (原用限价 current × 0.998)
- Prompt 完整移植,JSON cleanup 逻辑保留
- direction='short' 自动降级 (原直接拒绝)
- SL/TP 由 system_settings 动态控制 (原硬编码 5% / 2%)

#### 3.1.6 [已删除] U 本位破位策略 u_coin_style_trader_service.py

该文件已在 git 中标记为 deleted。historic source='u_coin_style' 仍可能存在于早期数据中,**当前系统不再使用此策略**。

#### 3.1.7 Gemini 探索 (app/services/gemini_explore_worker.py) [v3.5 新增]

**FR-GE01** 调度方式 (v3.5 从 main.py 移至 scheduler.py)
- 由 `app/scheduler.py` 统一调度,每 6 小时一轮
- `system_settings.gemini_explore_enabled` kill switch (默认 0)
- scheduler 启动时立即自动执行一轮 (15 秒延迟)
- 独立后台线程运行,不阻塞 scheduler 主循环

**FR-GE02** 候选池采集
- **极端波动池**: 24h 涨/跌各取 top 20, |change_24h| ≥ 15%
- **中等波动池**: 24h 涨/跌各取 top 8, |change_24h| ∈ [3%, 15%], 成交额 ≥ 500 万 USDT
- 排除已在极端池的 symbol
- 数据优先从 `data_cache.market_movers_snapshot` 读取

**FR-GE03** Gemini 分析
- 调用 `google.genai` SDK (新版),默认模型 `gemini-3-flash-preview`
- 分析 prompt 包含: 市场概况 + Big4 趋势 + 每 symbol 多项技术指标
- 返回 JSON: symbol / category (red_swan/black_swan/bullish/bearish/neutral) / confidence (0-1) / catalyst / data_signal / risk_note

**FR-GE04** 开仓条件 (v3.5 接入实盘)
- 类别映射: red_swan/bullish → LONG; black_swan/bearish → SHORT
- 校准表置信度 ≥ 0.50 方可开仓
- Big4 闸门: BEARISH/STRONG_BEARISH 禁 LONG; BULLISH/STRONG_BULLISH 禁 SHORT
- 同 symbol+side 已有 OPEN gemini_explore 仓位跳过
- 最大 20 仓 (EXPLORE_MAX_POSITIONS)

**FR-GE05** 仓位参数
- account_id = 2 (U 本位模拟盘)
- SL = 5%, TP = 15%, 杠杆 = 3x
- 保证金 = 500U, 持仓 72 小时
- 硬 SL/TP 由 PositionSLTPMonitor 兜底,跳过智能平仓检测

**FR-GE06** 实盘同步 (v3.5 新增)
- 模拟单开仓成功后,立即调用 `_sync_to_live()` 同步实盘
- 闸门: `system_settings.live_trading_enabled` (统一实盘开关)
- 遍历所有 `user_api_keys.status='active'` 的账号
- 调用 `BinanceFuturesEngine.open_position()`,通过 `paper_position_id` 关联实盘单
- 实盘持仓限每账号 20 仓 (BinanceFuturesEngine 硬编码)

#### 3.1.8 Gemini 预测 (app/services/gemini_predictor.py) [v3.5 新增]

**FR-GP01** 调度方式 (v3.5 从 main.py 移至 scheduler.py)
- 由 `app/scheduler.py` 统一调度,每 12 小时一轮
- `system_settings.gemini_predict_enabled` kill switch (默认 0)
- scheduler 启动时立即自动执行一轮
- 独立后台线程运行

**FR-GP02** 预测逻辑
- 取 TOP50 交易对,调用 Gemini 预测未来方向
- 返回 LONG/SHORT 及 confidence 分数
- 写入 `gemini_predict_runs` / `gemini_predict_verdicts` 表

#### 3.1.9 市场情绪分析 (app/services/gemini_sentiment_analyzer.py) [v3.5 新增]

**FR-GS01** 调度方式 (v3.5 从 main.py 移至 scheduler.py)
- 由 `app/scheduler.py` 统一调度,每 8 小时一轮
- `system_settings.gemini_sentiment_enabled` kill switch (默认 1)
- scheduler 启动时立即自动执行一轮
- 独立后台线程运行

**FR-GS02** 分析内容
- **市场情绪标签**: bullish/bearish/neutral/fear/greed + 评分
- **大方向判断**: 综合市场条件的中文分析 (TEXT 字段)
- **川普分析**: 川普推特/讲话对 crypto 的情绪影响 + 评分
- 使用新版 `google.genai` SDK,配置超时 120 秒
- 写入 `gemini_sentiment_runs` 表

**FR-GS03** 数据保护
- 所有 varchar 字段代码级截断,避免 `Data too long` 错误
- market_direction_verdict 列已从 VARCHAR(100) 改为 TEXT (迁移脚本 044)

#### 3.1.10 DeepSeek 探索 (app/services/deepseek_explore_worker.py) [v3.6 新增]

**FR-DE01** 调度方式
- 由 `app/scheduler.py` 统一调度,每 6 小时一轮 (与 Gemini 探索同步)
- `system_settings.deepseek_explore_enabled` kill switch (默认 0)
- scheduler 启动时立即自动执行一轮

**FR-DE02** 技术实现
- 使用 `openai` SDK 调用 DeepSeek API (兼容 OpenAI 协议)
- 默认模型: `deepseek-chat`
- API Key 和模型配置来自 `.env` (`DEEPSEEK_API_KEY` / `DEEPSEEK_MODEL`)
- 候选池逻辑与 Gemini 探索相同 (极端波动池 + 中等波动池)

**FR-DE03** 仓位参数
- 与 Gemini 探索完全一致: SL=5%, TP=15%, 杠杆=3x, 保证金=500U, 持仓 72h
- source = `deepseek_explore`
- **不同步实盘** (live_trading_enabled 不影响 DeepSeek 探索)

#### 3.1.11 DeepSeek 预测 (app/services/deepseek_predictor.py) [v3.6 新增]

**FR-DP01** 调度方式
- 由 `app/scheduler.py` 统一调度,每 6 小时一轮
- `system_settings.deepseek_predict_enabled` kill switch (默认 0)
- 使用 `openai` SDK 调用 DeepSeek API

**FR-DP02** 预测逻辑
- 取 TOP50 交易对,调用 DeepSeek 预测未来方向
- 返回 LONG/SHORT 及 confidence 分数
- source = `deepseek_predict`
- **不同步实盘**

#### 3.1.12 交易操作手册 (app/api/trading_manual_api.py) [v3.6 新增]

**FR-TM01** 功能概述
- 新增 Web 页面 `/trading-manual` (桌面) 和 `/m/manual` (手机),提供完整交易学习手册
- 9 章内容存储在 `trading_manual_chapters` 表,支持在线编辑
- 数据库驱动:所有章节内容和元数据 (标题/图标/配色) 从数据库读取

**FR-TM02** AI 问答功能
- 集成 Gemini / DeepSeek AI 问答对话框
- 用户可切换 AI 来源 (Gemini / DeepSeek)
- 支持快捷提问按钮 (识别顶底/风险控制/买卖策略等)
- Q&A 记录保存在 `trading_manual_qa` 表,支持编辑、保存、删除

**FR-TM03** 数据库表

```sql
CREATE TABLE trading_manual_chapters (
    id              INT AUTO_INCREMENT PRIMARY KEY,
    chapter_number  INT NOT NULL COMMENT '章节号',
    title           VARCHAR(100) NOT NULL,
    subtitle        VARCHAR(200) DEFAULT '',
    content         MEDIUMTEXT COMMENT '章节内容(HTML)',
    icon            VARCHAR(50) DEFAULT 'menu_book',
    color           VARCHAR(20) DEFAULT 'primary',
    sort_order      INT DEFAULT 0,
    updated_at      DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    UNIQUE KEY uk_chapter (chapter_number)
);

CREATE TABLE trading_manual_qa (
    id              INT AUTO_INCREMENT PRIMARY KEY,
    question        TEXT NOT NULL,
    answer          TEXT COMMENT '编辑后的回答',
    raw_answer      TEXT COMMENT 'AI原始回答(JSON)',
    source          VARCHAR(20) DEFAULT 'gemini',
    status          VARCHAR(20) DEFAULT 'draft',
    created_at      DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at      DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
);
```

---

### 3.2 实盘交易

**FR-L01** 实盘开关
- `system_settings.live_trading_enabled = 1` 开启实盘
- 实盘持仓记录在 live_futures_positions 表,与模拟盘 futures_positions 分离
- **当前值 = 0**,系统不主动下实盘

**FR-L02** 实盘执行路径
- 主策略 → smart_trader_service 内部走 binance_futures_engine.open_position()
- 多策略 / BTC动量 / 预测器 → 各自策略文件的 _sync_live() 调用同一引擎
- 多账号 (user_api_keys.status='active' 全部) 同时下单

**FR-L03** 实盘限制
- 实盘开仓必须在 Top50 名单内
- 每账号 max_position_value / max_leverage / max_daily_loss 限额
- 每账号 OPEN 仓位上限 20 (硬编码 MAX_LIVE_POSITIONS=20)

**FR-L04** 实盘对账 (reconcile_live_positions, 每 5 分钟)
- 方向 1: DB OPEN 但交易所已平 → 标记 CLOSED, close_reason='reconcile_closed'
  - 价格兜底链 (2026-05-14 修复后): WS → exchange_price_map → 5m K 线 (陈旧带告警)
  - PnL 计算失败时 logger.error 显式标记错误数据
- 方向 2: 交易所有持仓但 DB 没有 → 通过 sync_positions_from_binance 补录,source='binance_sync'
  - **用户在 Binance app 直接开仓,5 分钟内会被同步进 DB,但 source 不受 Top50/Big4 过滤约束**

---

### 3.3 数据采集

**FR-D01** K 线数据
- 每 5 分钟采集全部交易对的 5m / 15m / 1h / 4h / 1d K 线
- 存入 kline_data 表 (open_time 为毫秒时间戳)
- **1m K 线已停采** (停在 2026-01-22),所有策略不应再依赖 1m

**FR-D02** K 线评分
- 每 5 分钟通过存储过程更新 coin_kline_scores 表
- 字段:total_score / direction / strength_level

**FR-D03** ETF 资金流
- 每天 06:45 (本地时间) 从 Farside (farside.co.uk) 同步 BTC/ETH ETF 日度资金流
- 存入 crypto_etf_flows,并更新 crypto_etf_daily_summary

**FR-D04** 企业金库
- 每天 07:30 (本地时间) 从 BitcoinTreasuries (bitcointreasuries.net) 同步 Top 100 上市公司 BTC 持仓
- 存入 corporate_treasury_companies / corporate_treasury_purchases

**FR-D05** 其他数据
- 资金费率：每 5 分钟 (funding_rate_data)
- Binance 公告：每 30 分钟
- 区块链 Gas 统计：每天 01:00
- 24H 价格统计：实时缓存于 price_stats_24h

---

### 3.4 Big4 趋势检测 (app/services/big4_trend_detector.py V3 算法)

**FR-G01** 单币计算 (_analyze_symbol_v2)
- 取最近 16 根 15M K 线 (=4 小时窗口)
- 计算 bearish_count (阴线数), bullish_count (阳线数), price_change_pct (区间涨跌)
- BTC 特别阈值:0.2%,其他币:0.3% (BULLISH/BEARISH 判定时)

**FR-G02** 单币信号等级

| 条件 | 信号 |
|------|------|
| 阴线≥11 且区间≤-0.5% | STRONG_BEARISH |
| 阴线≥9 且区间≤-0.3% (BTC -0.2%) | BEARISH |
| 阳线≥11 且区间≥+0.5% | STRONG_BULLISH |
| 阳线≥9 且区间≥+0.3% (BTC +0.2%) | BULLISH |
| 其余 | NEUTRAL |

reason 字段格式:`15M强空头(11/16阴线,区间-1.59%)`

**FR-G03** Big4 合成
- 权重:BTC 50% + ETH 30% + BNB 10% + SOL 10%
- 加权阈值 > 60% 触发 BULLISH / BEARISH;阴阳同时强势时取强者
- 写入 big4_trend_history 表

**FR-G04** 紧急干预 (_detect_emergency_reversal)
- 1H 级别:最近 4 小时 BTC/ETH/BNB/SOL 任一跌/涨 > 5% → 触底/触顶
- 15M 级别:1H 长下影线 (>3%) + 后续 15M 连续 3 根阳线 → 深 V 触底
- 触发后写 emergency_intervention 表 (block_long / block_short / expires_at)
- 配合反弹窗口 bounce_window:触底后 45 分钟内允许全市场开多

**FR-G05** 震荡市检测 (_detect_choppy_market)
- 过去 4 小时 Big4 信号反复切换 → is_choppy=True
- 主循环检测到 is_choppy 且非 STRONG 信号时跳过本轮开仓

---

### 3.7 数据库预计算缓存层 (v3.5 新增)

**FR-C01** data_cache 数据库
- 独立 schema `data_cache`,与 `binance-data` 同实例
- 5 张缓存表,由 scheduler 定时刷新
- 应用层优先读缓存,缓存不可用时回退直接查 binance-data

**FR-C02** 缓存表与刷新频率

| 表名 | 刷新频率 | 内容 |
|------|---------|------|
| market_snapshot | 每 1 分钟 | 核心币种最新价/24h 涨跌/Big4 信号/恐惧贪婪指数/BTC 占比 |
| market_movers_snapshot | 每 5 分钟 | 涨幅/跌幅 top 20 + 资金费率极端值 + 成交量异动 |
| position_stats_snapshot | 每 30 分钟 | 各 source 的 30d/7d/今日盈亏/胜率/多空分布 |
| candidate_pool_snapshot | 每 30 分钟 | 各策略白名单 + RealMotion 候选池 |
| settings_cache | 每 1 分钟 | system_settings 全量缓存 |

**FR-C03** 缓存刷新实现
- 在 `app/services/data_cache_service.py` 中统一管理
- 使用 `REPLACE INTO` 写入,无主键冲突风险
- 首次启动 scheduler 时按顺序执行初始采集

---

### 3.8 调度引擎 (v3.5 scheduler.py 重组)

**FR-S01** 独立进程
- `app/scheduler.py` 作为独立系统服务 (`crypto-scheduler`) 运行
- systemd 管理,自动重启 (crypto-scheduler.service)
- PID 文件锁防止多实例 (`app/utils/pid_lock.py`)

**FR-S02** 调度任务清单

| 任务 | 频率 | 说明 |
|------|------|------|
| 价格采集 | 持续运行 | WebSocket + REST fallback |
| 数据库保活 | 每 30 秒 | 防止 MySQL 闲置断连 |
| 缓存刷新 (market) | 每 1 分钟 | data_cache.market_snapshot |
| 缓存刷新 (movers) | 每 5 分钟 | data_cache.market_movers_snapshot |
| 缓存刷新 (stats) | 每 30 分钟 | data_cache.position_stats_snapshot |
| 缓存刷新 (candidate) | 每 30 分钟 | data_cache.candidate_pool_snapshot |
| 缓存刷新 (settings) | 每 1 分钟 | data_cache.settings_cache |
| Gemini 探索 | 每 6 小时 | gemini_explore_worker |
| Gemini 预测 | 每 12 小时 | gemini_predictor |
| 市场情绪分析 | 每 8 小时 | gemini_sentiment_analyzer |
| ETF 同步 | 每天 06:45 | farside_etf_sync |
| 金库同步 | 每天 07:30 | bitcointreasuries_sync |

**FR-S03** 后台线程执行
- 所有耗时任务在独立 `threading.Thread` 中运行
- 避免阻塞 schedule 主循环 (单线程默认定时器)
- 异常由任务内部捕获,不影响其他定时任务

---

---

### 3.9 信号评分体系

**FR-SS01** 评分组件 (SignalScoreV2Service)
- 共 14 个 V1 信号组件 (RSI、MACD、布林带、EMA、量能等)
- LONG 满分 217,SHORT 满分 243
- 权重存储在 signal_scoring_weights 表 (strategy_type 区分:'default' 主策略,其他扩展)

**FR-SS02** 权重自动优化 (ScoringWeightOptimizer)
- 每日 00:00 UTC 运行
- 调整幅度 ±2~3,权重范围 [5, 30]
- 需 ≥5 笔样本才调整;仅针对 strategy_type='default'
- Big4 过滤:熊市多头亏损不计入多头权重评估 (commit 91eb608)

**FR-SS03** 信号黑名单 (signal_blacklist)
- 对特定信号类型 + 方向加黑名单
- 黑名单信号不参与评分
- 5 分钟内存缓存,自动刷新

**FR-SS04** 市场状态机 (Big4RegimeMonitor)
- 每小时检测 Big4 过去 48 小时趋势分布
- 自动更新 system_settings.allow_long / allow_short
- 熊市持续时关闭 allow_long,反之亦然

---

### 3.10 Web 管理界面

**FR-W01** 持仓监控页面
- U 本位合约页:持仓列表,含开仓信号
- 币本位合约页:币本位持仓
- 实盘页:实盘持仓 + 账户余额

**FR-W02** 数据分析页面
- 主仪表盘:实时 PnL、胜率、各策略状态
- 技术信号:当前各交易对信号强度
- 合约回顾:历史交易统计、每日盈亏、信号有效性
- ETF 资金流:BTC/ETH ETF 净流入趋势
- 企业金库:上市公司 BTC 持仓变化

**FR-W03** 系统管理页面
- 系统设置:全局开关 (allow_long、allow_short、live_trading_enabled、stop_loss_pct、take_profit_pct、max_hold_hours 等)
- API Keys:多账号 Binance API 密钥管理
- 信号黑名单:管理不良信号
- 交易对评级:调整 symbol 的风险等级 (Level 0~3)
- Top50:展示高胜率交易对排名

**FR-W04** 权限控制
- JWT 认证,访问受保护页面需登录
- 管理员可管理用户账号

---

### 3.11 通知系统

**FR-N01** Telegram 通知
- 开仓:币种、方向、保证金、评分、策略名
- 平仓:盈亏、持仓时间、平仓原因
- 实盘下单:BTC动量、预测器、多策略实盘下单后推送 (含 strategy_name 标注)
- 熔断触发通知:盈利/亏损熔断、多策略熔断
- 每日复盘报告:00:00 UTC 发送

**FR-N02** 报告内容
- 当日总盈亏、胜率、开仓次数
- 各信号组件有效性排名
- 权重调整记录

---

### 3.12 保证金与评级规则

**FR-M01** 分级保证金

| 评级 | 保证金/笔 | 说明 |
|------|---------|------|
| Level 0 (正常) | 400U | 默认 |
| Level 1 (限制) | 100U | 近期表现较差 |
| Level 2 (严格) | 50U | 高风险交易对 |
| Level 3 (禁止) | 不开仓 | 永久黑名单 |

**FR-M02** Top50 过滤
- live_trading_enabled=1 时,实盘开仓必须在 top_performing_symbols 内
- 每日更新排名 (top_n=50),按总盈利和胜率综合排名
- **binance_sync 来源 (用户手动 Binance app 操作) 不受此约束**

---

## 4. 非功能需求

### 4.1 性能

| 指标 | 要求 |
|------|------|
| 信号扫描延迟 | 单次全量扫描 (290 个交易对) ≤60 秒 |
| 价格推送延迟 | WebSocket 实时价格延迟 ≤500ms |
| API 响应时间 | Web API 95% 请求 ≤2 秒 |
| 数据库连接池 | pool_size=20,max_overflow=30 |

### 4.2 可用性

- 自动重启:主交易服务异常退出后需手动重启 (无自动重启)
- 智能平仓监控:SmartExitOptimizer 崩溃时 smart_trader_service 每分钟检测并自动重启
- 数据库连接:连接泄漏防护 (_get_connection() 必须手动 close())
- 实盘对账:每 5 分钟双向对账,补漏 + 清理孤儿单

### 4.3 安全

- API 密钥加密存储 (user_api_keys 表)
- .env 文件不进 git (含数据库密码、Binance 密钥)
- JWT 认证保护所有管理接口
- 禁止在代码中硬编码密钥
- 数据库密码必须用 dotenv_values('.env') 读取,不读系统环境变量 (防多版本部署污染,commit bb97892)

### 4.4 可维护性

- 代码改动后必须语法检查:`python -c "import ast; ast.parse(open('file.py',encoding='utf-8').read())"`
- 代码改动后必须重启服务 (Python 不热重载)
- 单文件不超过 3000 行 (smart_trader_service.py 已 4100 行需重构)
- 所有 except 块必须 logger.error 记录,禁止空 pass
- 三个独立进程分别重启:smart_trader_service.py / coin_futures_trader_service.py / app/main.py

### 4.5 数据保留

- K 线数据:按配置保留周期 (config.yaml retention 字段)
- 持仓记录:永久保留
- 日志:按 logs/ 目录配置保留

---

## 5. 约束条件

- 运行平台:Windows 10,Python 3.10+ venv 环境
- 数据库:MySQL 8.0 (远程 AWS 实例)
- 交易所:仅支持 Binance 合约
- 服务独立运行:三个进程互不依赖
- **1m K 线不可用** (停采于 2026-01-22),所有策略必须用 5m+ 数据
- 实盘持仓与模拟持仓分表
- Windows 终端 GBK 编码,代码禁用 emoji,日志输出统一 ASCII/中文

---

## 6. 数据字典 (关键字段说明)

| 表名 | 关键字段 | 说明 |
|------|---------|------|
| futures_positions | account_id | 2=U本位模拟,3=币本位模拟 |
| futures_positions | source | 策略来源标识,见下表 |
| futures_positions | planned_close_time | SmartExitOptimizer 定时平仓触发时间 |
| futures_positions | notes | 平仓原因 (无 close_reason 列) |
| system_settings | setting_key/setting_value | 全局开关 (注意列名不是 key/value) |
| signal_scoring_weights | strategy_type | 'default' = 主策略 |
| trading_symbol_rating | rating_level | 0=正常, 1=限制, 2=严格, 3=禁止 |
| live_futures_positions | account_id | 对应 user_api_keys.id (实盘多账号) |
| live_futures_positions | paper_position_id | 关联 futures_positions.id (策略同步过来) |
| live_futures_positions | close_reason | reconcile_closed / manual_close_stale / manual 等 |
| big4_trend_history | overall_signal | STRONG_BULLISH/BULLISH/NEUTRAL/BEARISH/STRONG_BEARISH |
| emergency_intervention | intervention_type | BOTTOM_BOUNCE / TOP_REVERSAL |
| emergency_intervention | block_long / block_short | 紧急封禁方向 |
| bounce_window | window_end | 触底后允许做多的窗口结束时间 (45min) |
| top_performing_symbols | rank_score | 综合排名,Top50 实盘必备 |

### futures_positions.source 取值 (实测)

| source | 策略 | 状态 |
|--------|------|------|
| smart_trader | U本位主策略 (signal_confirm/trend_follow 实际写入此值) | 在跑 |
| BTC_MOMENTUM | BTC 动量跟随 | 在跑 (4月底后触发条件未满足) |
| PREDICTOR | 市场预测器 | 在跑 |
| s1_early_long | 多策略 S1 早期做多 | 测试中 |
| s2_pullback_long | 多策略 S2 无量回调 | 测试中 |
| s3_top_short | 多策略 S3 顶部做空 | 测试中 |
| s4_rebound_short | 多策略 S4 反弹衰竭 | 测试中 |
| s5_large_oversold | 多策略 S5 大币超卖 | 测试中 |
| s6_vol_spike | 多策略 S6 量能异动 | **已清理** (5月未开仓) |
| s7_ma_support | 多策略 S7 均线支撑 | **已清理** (5月未开仓) |
| s8_topshort | 多策略 S8 顶部反转 [v3.2] | 默认 OFF (kill switch) |
| s9_gemini_ai | 多策略 S9 Gemini AI | 默认 OFF (kill switch) |
| gemini_explore | Gemini 探索 (v3.5) | 在跑,默认 OFF |
| gemini_predict | Gemini 预测 (v3.5) | 在跑,默认 OFF |
| deepseek_explore | DeepSeek 探索 (v3.6) | 在跑,默认 OFF |
| deepseek_predict | DeepSeek 预测 (v3.6) | 在跑,默认 OFF |
| manual | 手动开仓 | - |
| smart_trader_batch / batch_v2 | 历史旧策略 | 已下线 (最后 2026-02-21) |
| 15M_BREAKOUT | 历史破位策略 | 已下线 (2026-03-25) |
| u_coin_style | 历史 U 本位破位 | 已下线 (文件已删除) |

### live_futures_positions.source 取值 (实测)

| source | 含义 |
|--------|------|
| binance_sync | 用户在 Binance app 直接开仓后,reconcile 自动补录 |
| smart_trader_sync | 主策略实盘同步 |
| strategy_sync | 历史多策略实盘同步 (2025-12 ~ 2026-04,已停产生) |
| limit_order_sync | 限价单同步 |
| BTC_MOMENTUM / PREDICTOR | 策略直接下实盘的 source |
| manual | 手动 |

---

## v3.7 修订记录 (2026-06-21) — 中线策略 · 限价偏移 · 持仓顾问 · AI 平仓

相对 v3.6 / 2026-06-18 需求文档（`docs/REQUIREMENTS_LOGIC_ZH.md` v4.0）主要变化:

1. **中线量化策略**（`gemini/deepseek_midline_long/short`）: L0/L1 池 + 24×1D/60×1H 扫描；**非 LLM**；跳过开/持仓顾问；**不参与 SmartExit**；`position_sl_tp_monitor` 负责硬 SL/TP、15 天到期（**无 ai-trail-tp**）
2. **限价偏移分流**（`paper_limit_entry.py`）: 中线固定 **做多−3%/做空+3%**；探索/预测/smart_trader 等仍读 `system_settings.paper_limit_*_offset_pct`（0.1~1%）
3. **中线限价超时**: 6h（`MIDLINE_LIMIT_TIMEOUT_MINUTES=360`）
4. **PaperSync**: 模拟限价/转市价成交后立即 `sync_filled_order_now`；5 分钟同步窗不变
5. **持仓顾问**: scheduler **15min** tick；每仓 **15min** 复审；浮盈转亏 **urgent**；盈利 sell ROI **+8%** + 15m 反向≥4
6. **AI ai-trail-tp**: peak 价格收益 ≥3% 后回撤 ≥1% 程序化平仓（探索/预测；**不含中线**）
7. **实盘**: 中线纳入 `LIVE_SYNC_SOURCES`；开仓须 **L0** 白名单；保证金 API `max_position_value`
8. **L0 评级放宽**: 300U/40% 或 100U/45%；L3 默认不拦模拟仓

权威细节以 `docs/REQUIREMENTS_LOGIC_ZH.md` 为准。

---

## v3.5 修订记录 (2026-05-24) — 数据缓存层 + Gemini 独立调度 + Gemini 探索实盘接入

相对 v3.4 主要变化:

1. **数据库预计算缓存层 (data_cache)**: 新增 5 张缓存表 (market_snapshot / market_movers_snapshot / position_stats_snapshot / candidate_pool_snapshot / settings_cache),由 scheduler 定时刷新,应用层优先读缓存
2. **system_settings 本地 TTL 缓存**: 重构 system_settings_loader,使用 data_cache.settings_cache + 60 秒内存缓存,替代 50+ 处直接 MySQL 查询
3. **scheduler 重组**: 从 main.py 分离为独立进程,统一管理所有定时任务 (缓存刷新 + Gemini 系列 + ETF/金库同步)
4. **Gemini 系列任务迁移**: gemini_explore、gemini_predict、gemini_sentiment 从 main.py 移至 scheduler.py,后台线程运行,启动时立即执行一轮
5. **gemini_sentiment SDK 修复**: 废弃 google.generativeai (旧版 SDK),改用 google.genai 新版;加上 varchar 截断保护,避免 Data too long 错误
6. **Gemini 探索接入实盘**: 模拟单开仓后立即走 _sync_to_live(),受统一闸门 live_trading_enabled 控制;遍历所有 active API Key 调用 BinanceFuturesEngine.open_position()

---

## v3.4 修订记录 (2026-05-21) — 信号评分与风控系统性优化 (11项改进)

相对 v3.3 主要变化:

1. **C1: 放宽Big4准入规则**: BULLISH/BEARISH时阈值+5, NEUTRAL时阈值+15且要求score≥80 (原直接封杀非STRONG方向)
2. **C2: 信号拥挤度惩罚**: 同方向超过3个信号组件时,每多一个扣3分,最多扣15分
3. **C3: 权重优化器支持降权**: 组件 performance_score<-5 时允许-2/-3降权
4. **C4: 按评分差异化仓位大小**: score<65→x0.6, 65-80→x0.8, 80-100→x1.0, 100-130→x1.2, >130→x0.8
5. **C5: 动态止损止盈**: 弱信号(score<65) SL=5%/TP=6%; 强信号(score≥80) SL=2%/TP=10%
6. **M1: 羊群防御对称化**: SHORT做空密集(≥80%)时也加阈值+5,与LONG对称
7. **M2: 反弹窗口上限**: 同一反弹窗口最多开10单
8. **M3: 退出原因增加close_reason_code枚举**: 每种平仓reason映射到短码
9. **M4: V2数据过期保守模式**: V2评分不可用或>15min陈旧时,阈值+10
10. **L1: 监控频率随持仓时间递减**: <30min:1s, 30min-2h:3s, 2h-4h:5s, >4h:10s
11. **L3: 资金费率动态阈值**: 从固定±0.1%改为24h均值±2σ

## v3.3 修订记录 (2026-05-17)

相对 v3.2 主要变化 (多策略统一 + 风控调整 + 手续费优化):

1. **S1-S9 参数统一** (commit 28ffa2f):
   - 杠杆全部 5x (S2 从 10x 调整)
   - 模拟保证金统一 500U (原 200-300U 不等)
   - SL/TP/持仓时间统一从 `system_settings.stop_loss_pct/take_profit_pct/max_hold_hours` 读
   - 实盘保证金由 `user_api_keys.max_position_value` 控制
2. **S8/S9 加 kill switch** (commit d52eb5a / 9e7e643):
   - `s8_topshort_enabled` / `s9_gemini_ai_enabled` 默认 OFF
   - 前端系统设置页 (PC + 移动端) 加独立开关
3. **多策略 TOP50 过滤** (v3.3 新增): `_is_symbol_in_top50` 在 `_sync_live` 入口处检查;实盘卡 TOP50,模拟不卡
4. **Gemini 实盘持仓顾问** (commit ff61c1a):
   - 实盘 OPEN ≥ 4h 的 U 本位单,每 1h 调 Gemini 判 hold/observe/sell
   - sell → BinanceFuturesEngine.close_position_direct
   - 默认 OFF,kill switch `gemini_position_advisor_enabled`
5. **极端止损放宽** (commit 7dfae49):
   - SmartExitOptimizer P5 极端止损 ROI -10% → -15%
   - 开仓 30 分钟豁免期 (避开建仓初期插针)
6. **启用移动止盈** (commit f692cb3):
   - 价格 +6% 激活 / 峰值回撤 1% 平仓 (与固定 TP 8% 协同)
   - 移动止损暂未启用
7. **手续费优化** (commit 5704f0f):
   - 同币冷却 15min → **30min** (5月数据 221/346=64% 重复开仓发生在 15-30 min 内)
   - 取消 `multi_strategy.run_fast()` (5月 S1-S9 一笔未开,run_fast 仅空扫描浪费 CPU)
   - run_slow 内同时跑 S1-S9 全部 (S9 内部 6h 限速)
8. **PID 锁修复** (commit 05db976): Linux subprocess UnboundLocalError 修
9. **SmartExitOptimizer 取价路径加强** (commit 0e8799a): delisted 币 -1121 错误处理 + DB 5m 兜底

## v3.2 修订记录 (2026-05-16)

相对 v3.1 主要变化 (test3 整合 + S8/S9 + 部署增强):

1. **集成 test3 系统**: 把 d:/test3 整套策略合并到 d:/test2 (commit 75282b0 + c351f62)
   - 拷 7 个文件: strategy_live + strategy_bigmid + state_db + gemini_swan_worker + paper_limit_sync + position_sl_tp_monitor + securities_filter
   - 所有 HTTP API_BASE 从 9021 改为 9020 (共享 test2 的 FastAPI)
2. **新增策略 S8 顶部反转做空** (s8_topshort): 集成 strategy_live.py 的 topshort 逻辑进 multi_strategy_service.py
3. **新增策略 S9 Gemini AI 抄底反转** (s9_gemini_ai): 集成 strategy_bigmid.py 的 Gemini 决策进 multi_strategy_service.py
4. **strategy_live/bigmid 进程化** → **进 multi_strategy_service 函数化**: 减 2 个独立进程,统一调度
5. **live_trading_enabled 守卫加强** (commit b669473):
   - smart_exit_optimizer._close_live_positions_on_exchange 加硬门
   - futures_trading_engine + coin_futures_trading_engine 加硬门
   - **删除危险的 symbol+direction 模糊 fallback** (会误平用户手动开的 binance_sync 单)
6. **system_settings.take_profit_pct 从 0.03 调到 0.05** (R:R 1.5 → 2.5)
7. **禁用 3 条极端 signal_threshold_overrides** (min_score>=120 的 id 14/18/35)
8. **新增 PID 文件锁** (app/utils/pid_lock.py): 5 个入口脚本启动时自检
9. **新增 healthcheck.py 脚本**: 6 进程健康检查 + JSON 输出 + exit code
10. **新增 systemd unit 模板** (deploy/systemd/): 6 个 .service 文件 + README
11. **Phase 5 归档脚本** (deploy/archive_test3.sh + phase5_verification_checklist.sql): test3 归档前置验证 (dry-run + 多重确认)
12. **进程数变化**: 6 个独立进程 (v3.1) → **4 个核心进程** (v3.2)
    - 删: strategy_live + strategy_bigmid (合并进 smart_trader_service 的 S8/S9)
    - 保留: app/main.py / smart_trader_service / coin_futures_trader / fast_collector
13. **Gemini AI 依赖**: 需要 pip install google-genai + .env 配 GEMINI_API_KEY/GEMINI_MODEL
14. **历史教训记录**: 两个 fast_collector 同跑触发 Binance IP ban (-1003),促成 PID 锁机制

---

## v3.1 修订记录 (2026-05-14)

相对 v3.0 主要校正:

1. **Big4 算法**:V3 实际阈值为 11/9 (STRONG/普通) + BTC 特殊阈值 0.2% / 普通币 0.3%,v3.0 文档写的 10/0.5% 不准确
2. **SL/TP 默认值**:v3.0 写 2%/6%,实际 system_settings 当前为 2%/3%,代码默认 BTC_MOMENTUM/PREDICTOR 是 2%/5%
3. **max_hold_hours**:v3.0 暗示 8 小时,实际 system_settings 当前为 3 小时
4. **PREDICTOR 单数**:v3.0 写"最多 5 单",代码注释明确"不限数量",实际跑出 1217 单
5. **新增多策略 S1-S7**:v3.0 完全没提的 7 个策略,实际跑在 multi_strategy_service.py
6. **删除 u_coin_style**:文件已删除,v3.0 仍包含此策略
7. **修正 source 命名**:实际写入 'smart_trader' 而非 'signal_confirm'/'trend_follow' (代码预留但未启用)
8. **新增 binance_sync 机制**:用户 Binance app 直接开单的实盘单同步,不受系统过滤约束
9. **新增 reconcile_live_positions 双向对账**:每 5 分钟检查,v3.0 未文档化
10. **新增紧急干预 + 反弹窗口**:emergency_intervention + bounce_window 表
11. **新增市场状态机**:Big4RegimeMonitor 每小时调整 allow_long/allow_short
12. **新增 13 道关卡完整命名**:[REFRACTORY] / [HABITUATION] / [SIGNAL_DECAY] / [HERDING] / [FUNDING_VETO] / [PROFIT_GUARD] / [LOSS_GUARD] / [CHOPPY_MARKET]
13. **新增盈利/亏损熔断**:6H>1000U 暂停 / 3H<-300U 暂停
14. **SmartExitOptimizer 移动止盈**已在代码中关闭 (插针频繁触发)
15. **多策略 S1-S7 跳过动态止盈止损**,只用固定参数
16. **BTC_MOMENTUM 1m K 线 fallback bug 已修复** (2026-05-14 commit 11d6981) 改为 5m + 15 分钟新鲜度检查
17. **reconcile_closed PnL 静默写 0 bug 已修复** (同 commit) 加第三层 K 线兜底 + 错误数据告警

---

## v3.6 修订记录 (2026-05-28) — DeepSeek 探索/预测 + 操作手册 + Unknown 信号修复 + 清理无效策略 (7项改进)

相对 v3.5 主要变化:

1. **DeepSeek 探索** (deepseek_explore_worker.py): 克隆 Gemini 探索逻辑,使用 `openai` SDK 调用 DeepSeek API,每 6h 一轮,source = `deepseek_explore`,SL=5%/TP=15%/3x/500U/72h,**不与实盘同步**
2. **DeepSeek 预测** (deepseek_predictor.py): 克隆 Gemini 预测逻辑,TOP50 预测方向,每 6h 一轮,source = `deepseek_predict`,**不与实盘同步**
3. **DeepSeek 页面及导航**: 新增 `templates/deepseek_explore.html`,所有 PC/移动端导航加入 DeepSeek 入口
4. **修复 Unknown 信号**: `futures_review_api.py` 入口信号分析中,`gemini_predict`/`gemini_explore`/`deepseek_predict`/`deepseek_explore` 等 AI 来源的 `entry_signal_type` 为空时被归为"未知",修复为通过 `source` 字段反向推导;同时在 AI workers/predictors 的 INSERT 语句中显式设置 `entry_signal_type`
5. **禁用 CryptoPanic 新闻采集**: 因 CryptoPanic API 返回 404 (服务已下线),注释掉相关代码,降低无用日志噪音
6. **清理无效 S 策略 (S2/S3/S4/S7/S8)**: 5 月数据显示 S2/S3/S4/S7/S8 未产生有效开仓,将策略代码置为禁用/跳过,降低每轮扫描 CPU 消耗
7. **交易操作手册**: 新增 Web 页面 `/trading-manual` (桌面)和 `/m/manual` (手机),9 章完整交易学习内容数据库驱动,集成 Gemini/DeepSeek AI 问答对话框,支持 Q&A 编辑保存;新增 `trading_manual_api.py` (CRUD + AI 问答),注册 `app/main.py`;所有 26 个页面添加导航链接

---

*文档版本：v3.6 | 最后更新：2026-05-28*
